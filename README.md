# 大光路智慧社区小程序

Vue 3 + uni-app 工作区，包含两个独立构建、独立发布的微信小程序：住户端与商户端。当前全部展示数据均为 Mock，未接入真实登录、RuoYi-Vue-Pro 接口、权限、支付或微信发布。

## 结构

```text
apps/resident-miniapp/   # 首页｜服务｜商城｜我的
apps/merchant-miniapp/   # 工作台｜经营｜订单｜我的
packages/theme/          # 唯一 Design Token 与全局样式
packages/ui/             # 唯一公共组件与 SVG 图标
packages/common/         # 类型、Project 上下文工厂、工具、加载状态
packages/business-common/# 端级 Mock 适配器
```

两个 App 都通过本地链接引用 `packages` 的同一份源码；不以登录角色决定一级导航。住户首页摄影 Mock 仅在住户端静态资源中，商户端不会打包。

## 命令

```powershell
npm.cmd install --cache .npm-cache
npm.cmd run dev:resident:mp-weixin
npm.cmd run dev:merchant:mp-weixin
npm.cmd run build:resident:mp-weixin
npm.cmd run build:merchant:mp-weixin
npm.cmd run type-check
npm.cmd run check:boundaries
```

产物分别位于 `apps/resident-miniapp/dist/build/mp-weixin` 和 `apps/merchant-miniapp/dist/build/mp-weixin`。两份 `src/manifest.json` 的微信 AppID 仍为空，项目方配置后才能导入对应微信主体发布。

## 阶段 01.6 视觉收口

- 已按 Stitch 母版完成住户服务、商城、我的，以及商户工作台、经营、订单、我的的一级视觉骨架收口；住户首页继续作为冻结视觉基准。
- 公共层统一收口了页面背景、卡片边界/轻阴影、标题层级、商户导航头和 `FeatureUnavailable` 底部反馈；未开发入口不扩展业务，只在点击后进入统一不可用反馈。
- 两端所有页面复用的 `AppNavbar` 均固定在顶部；页面内容在其下方滚动，顶部栏继续按真实状态栏与微信胶囊区计算安全高度。
- 商户订单采用 B1-_4 + B1-_6 的 L2 组合，商户我的采用 B2-_10 + B2-_3 的 L2 组合；没有进入阶段 02，也没有新增阶段 02 业务页面或路由。
- 直接 Stitch 对照已完成；住户端与商户端均已在 390×844 窄屏 H5 预览中取得八张有效运行画面（含住户首页回归），并核对商品图片、页面背景、底栏与公共导航。微信开发者工具/真机仍需项目方完成最终设备验收。
- 本轮最小修复已补齐两个 BaseCard 注册缺口、Stitch 商品图本地资源、公共导航安全区与无效返回保护、商户资质入口与资格摘要、入口反馈及筛选计数；没有新增阶段 02 业务。
- 本轮最终尺寸修复已收口：商户经营新增商品视觉高 40px、筛选芯片与商品操作视觉高 36px，并通过外层热区保持至少 44px；商户导航实际渲染身份入口，首页商品卡加购保持 36px 视觉并扩展 44px 热区；两端一级“我的”移除返回语义，商城文案调整为中性生活化表达。

## 阶段 01.7｜商户端作业效率与信息密度优化

- 本轮仅重构商户端工作台、经营、订单三张一级高频作业页；商户“我的”和所有住户端页面保持原有页面结构，未进入阶段 02。
- 商户端以 Compact Density 收口：工作台压缩商户简报、待办和快捷入口，并将今日经营/资金摘要合为经营概览；经营页移除大面积审核提示与帮助区；订单页移除重复状态横条，保留订单号、状态、商品、客户、数量、履约、下单/承诺时间及金额。
- `AppNavbar` 与 `MerchantInfoCard` 新增仅商户页面传入的 `compact` 选项；小操作视觉高度 36px、外层有效热区至少 44px，未修改全局 `AppButton` 的 48px 主 CTA 规范。

## 阶段 02｜商户入驻与合作关系

- 商户端已新增入驻申请、审核结果、基本资料、门店列表/编辑、人员列表/编辑、资质列表/编辑、合作状态和经营资格共 11 个二级页面；正式入口集中在“我的”，未改变四个一级 Tab。
- `OperatorApplication` 与正式 `Operator` 分离；申请通过前只维护申请及审核历史，通过后才进入正式主体、首店和负责人体系。
- 门店仅作为经营/履约场所；人员按负责人、工作人员、服务人员和门店范围控制前端交互；最后一名有效负责人不可直接停用。
- 合作协议、项目合作和经营授权在商户端只读；经营资格页面分别展示业务经营资格、资金接入状态和正式交易资格，资金配置中不影响经营内容维护。
- 页面通过 `merchant-cooperation service → Mock Adapter` 访问统一业务契约。当前无真实后端接口，所有阶段 02 数据、操作和权限结果仍为内存 Mock，后续只替换 Adapter。
- R 页面按 B2-_2、B2-_3、B1-_1 的表单/解释型语言实现；门店、人员、资质三个 O 页面按 B1-_6 的品牌语言采用紧凑对象列表，操作热区保持至少 44px。
- 全端共享 `AppButton` 已按微信小程序操作语义统一主、次、轻量、危险和加载状态；表单页固定底栏采用“次操作 104px + 主操作自适应”布局，保存/提交不再以等宽按钮并列。

## 已验证

- `npm.cmd run type-check:resident`、`npm.cmd run type-check:merchant` 通过；边界检查通过。
- 双端 mp-weixin 构建通过，且 `app.json` 页面清单互相隔离。
- 双端 H5 发布构建通过；`node scripts/check-visual-shell.mjs` 通过图片资源、计数、资格入口、组件注册和范围守卫检查。
- 已保留住户首页既有视觉页面、Mock 摄影资源与共享组件样式。
- 阶段 01.7 已完成商户工作台、经营、订单的 390×844 H5 运行核对；微信开发者工具/真机安全区仍需项目方最终验收。
- 阶段 02 已通过商户/住户 type-check、双端 mp-weixin 构建和边界检查；两份生成的 `app.json` 页面清单保持端级隔离。
- 阶段 02 的 11 个页面已在 390×844 H5 运行预览中逐页检查；商户四个一级页和住户首页已回归。微信开发者工具、真实 AppID、真机大字体/键盘/拍照上传与真实接口仍需人工验证。
- 本轮已完成“编辑人员”“基本资料”和“商户人员”H5 操作按钮回归：主次操作层级、48px 表单 CTA、44px 紧凑操作热区和加载防重复符合当前微信小程序实现规范。
- 本轮已在住户“我的”与商户入驻长表单中完成向下滚动回归：顶部栏、返回入口、底部 Tab 和 Bottom Action 均保持固定，滚动内容不发生遮挡。

构建会输出 Sass legacy API / `@import` 弃用警告，不影响当前构建成功。
