# 大光路智慧社区小程序_开发交接文档

## 当前状态

当前为 Vue 3 + uni-app 双小程序工作区。住户端与商户端可分别构建微信小程序产物；数据仍是 Mock，真实登录、后端接口、权限、支付、微信 AppID 与发布均未完成。

阶段 01.6 已完成一级骨架视觉收口：住户服务、商城、我的，以及商户工作台、经营、订单、我的已按指定 Stitch 母版完成页面级调整；住户首页作为视觉基准保持冻结。本阶段没有进入阶段 02，也没有新增阶段 02 业务路由。

阶段 01.7 已完成商户端作业效率与信息密度优化：仅调整工作台、经营、订单三张一级高频作业页，商户“我的”和住户端页面保持原状。工作台收口为商户简报、待办、快捷操作、经营概览；经营页压缩商品状态与操作；订单页以订单卡直接呈现状态、履约信息和下一步操作。未新增路由、真实接口或阶段 02 业务。

阶段 02 已完成商户入驻与合作关系前端：新增 11 个商户端二级页面，覆盖入驻申请与审核结果、主体基本资料、门店、商户人员、资质、合作状态和经营资格。“我的”已接入正式入口，四个一级 Tab 保持冻结。当前阶段 02 全部走统一 Service 与内存 Mock Adapter，未接入真实后端、登录鉴权、文件上传或资金接口。

## 工程与路由

| App | 工程 | 一级页面 | mp-weixin 产物 |
| --- | --- | --- | --- |
| 住户端 | `apps/resident-miniapp` | 首页、服务、商城、我的 | `apps/resident-miniapp/dist/build/mp-weixin` |
| 商户端 | `apps/merchant-miniapp` | 工作台、经营、订单、我的 | `apps/merchant-miniapp/dist/build/mp-weixin` |

商户端阶段 02 二级路由：

| 业务组 | 路由 |
| --- | --- |
| 入驻申请 / 审核结果 | `/pages/onboarding/application/index`、`/pages/onboarding/result/index` |
| 基本资料 | `/pages/profile/basic/index` |
| 门店 | `/pages/stores/index`、`/pages/stores/editor/index` |
| 商户人员 | `/pages/staff/index`、`/pages/staff/editor/index` |
| 资质 | `/pages/qualifications/index`、`/pages/qualifications/editor/index` |
| 合作状态 / 经营资格 | `/pages/cooperation/status/index`、`/pages/qualification/status/index` |

`packages/theme`、`packages/ui`、`packages/common` 与 `packages/business-common` 是唯一共享源码。两个 App 通过本地链接映射共享目录，以兼容 uni-app 小程序编译器对 App 本地 `src` 路径的解析；不得复制共享组件。

住户端初始化 `currentProject/currentMember`；商户端初始化 `currentProject/currentOperatorApplication/currentOperator/currentStore/currentMerchantStaff`。申请与正式主体不可互换，门店不承担主体资格。`AppTabbar` 只渲染 App 注入的固定导航，不读取用户角色。

阶段 02 核心代码位置：

| 能力 | 位置 | 当前状态 |
| --- | --- | --- |
| 页面业务契约 | `packages/common/types/merchant-cooperation.ts` | 已实现 |
| 商户端 Service | `apps/merchant-miniapp/src/services/merchant-cooperation.ts` | 已实现，稳定页面调用入口 |
| Mock Adapter | `packages/business-common/mock/merchant-cooperation.ts` | 已实现，内存数据和操作 |
| 上下文与前端权限 | `apps/merchant-miniapp/src/stores/merchant-context.ts` | 已实现负责人/普通工作人员基础边界 |
| 表单与状态组件 | `packages/ui/components` | 新增 BottomActionBar、FormSection、FormField、StepProgress、EvidenceUploader、ResultState、QualificationStatusCard |
| 按钮与底部操作 | `packages/ui/components/AppButton.vue`、`BottomActionBar.vue` | 主/次/轻量/危险/加载语义；底栏次操作固定 104px、主操作自适应 |

经营资格严格分为业务经营资格、资金接入状态和正式交易资格。合作协议、项目合作与经营授权在商户端只读；商户端不能直接修改平台合作数据。资质更新后先进入待确认，未经平台确认不参与资格判断。最后一名有效负责人停用在 Adapter 层被拒绝；真实接入后同样规则必须由后端执行。

## 运行与检查

```powershell
npm.cmd run build:resident:mp-weixin
npm.cmd run build:merchant:mp-weixin
npm.cmd run type-check
npm.cmd run check:boundaries
```

构建脚本通过 `scripts/run-uni.mjs` 在启动 uni CLI 前设置目标平台，避免 CLI 首次加载时回退到 H5。当前双端 mp-weixin/H5 构建、类型检查、边界检查和视觉壳层守卫均已通过；微信开发者工具/真机视觉验收和真实 AppID 发布尚未执行。

阶段 02 已执行商户/住户 type-check、双端 mp-weixin 构建和边界检查；商户生成产物注册 4 个一级页及 11 个阶段 02 二级页，住户产物仍仅注册 4 个住户一级页。11 个阶段 02 页面已在 390×844 H5 中逐页运行核对，商户四个一级页和住户首页完成回归。Sass legacy API 与 `@import` 弃用信息仍为非阻断警告。

本阶段直接打开并对照了各指定 Stitch `screen.png`。修复依赖版本不一致后，已在 390×844 窄屏 H5 预览中取得八张有效运行画面：住户首页（回归）、服务、商城、我的，以及商户工作台、经营、订单、我的。H5 画面已核对页面背景、公共导航、商品图片、底栏和明显入口反馈；微信开发者工具/真机安全区仍需人工最终验收。

公共视觉收口集中在 `packages/ui/components`：`AppNavbar`、`BaseCard`、`SectionHeader`、`ProductCard`、`MerchantInfoCard` 与 `FeatureUnavailable`；页面局部仅保留各母版所需的布局、状态展示与 Mock 文案。未开发入口统一使用 `FeatureUnavailable`，不通过 Toast 暴露阶段内部说明。商品图片已由 Stitch 原生资源落地到两个 App 的静态目录，商户经营/订单筛选计数由当前 Mock 数据计算。本轮最终最小修复未改变全局主 CTA 规范，仅在商户经营页收口 40px/36px 视觉按钮并通过外层热区满足 44px 点击要求；`AppNavbar.showUser` 已实际渲染商户身份入口，`ProductCard` 首页加购保持 36px 视觉并扩展 44px 热区。阶段 01.7 为 `AppNavbar` 与 `MerchantInfoCard` 增加默认关闭的 `compact` 样式，仅工作台、经营、订单传入；住户端不传入该选项，不受 Compact Density 影响。`AppNavbar` 现为两端全页面固定顶部栏，保留自身高度避免内容遮挡，并继续使用状态栏和微信胶囊的动态安全区数据。

阶段 02 的 R 页面按 B2-_2、B2-_3、B1-_1 实现分步流程、表单分组、状态解释和 Bottom Action；门店、人员、资质三个 O 页面按 B1-_6 的视觉语言组织紧凑对象卡。H5 已验证页面构图和路由，微信开发者工具中的胶囊安全区、真机大字体、键盘避让、拍照/文件选择、网络异常以及真实后端权限仍需联调验收。

共享按钮体系遵循微信小程序的主、次、轻量、危险和加载操作语义：表单 CTA 固定 48px，加载状态通过原生 `button.loading` 防重复；固定底栏保留安全区、次操作固定 104px、主操作占余宽。商户 O 页面可将按钮视觉压缩为 36px，但外层热区仍为至少 44px。H5 已复核编辑人员、基本资料与商户人员列表的保存、取消、新增、编辑、停用等操作样式。

## 修订记录

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| 全端固定顶部栏 | 2026-09-17 | 共享 AppNavbar 改为 sticky 顶部导航；住户“我的”与商户入驻长表单下滑回归确认导航、返回入口、底部 Tab/Bottom Action 均稳定。 |
| 微信小程序按钮规范收口 | 2026-09-17 | 共享 AppButton 与 BottomActionBar 统一主次操作、加载/禁用/按下状态和安全区；阶段 02 表单页接入原生加载态，商户作业列表补齐紧凑按钮热区与按下反馈。 |
| 02 商户入驻与合作关系 | 2026-09-16 | 新增 11 个商户端二级页面、统一 Service/Mock 契约、申请/正式主体分层、门店人员资质管理、合作只读汇总和三层经营资格；完成双端构建、边界与 H5 回归，未进入阶段 03。 |
| 01.7 商户作业效率与信息密度优化 | 2026-09-16 | 仅重构商户工作台、经营、订单：压缩待办/快捷入口、合并经营概览、收口商品状态与订单卡字段/操作，新增商户可选紧凑导航与简报样式；未进入阶段 02。 |
| 01.6 最终最小修复 | 2026-09-15 | 收口商户经营按钮/筛选尺寸与点击热区、修复工作台资金摘要选择器、实现商户导航身份入口、统一两端一级“我的”语义并中性化商城文案；未进入阶段 02。 |
| 01.6 一级骨架视觉收口 | 2026-09-15 | 按 Stitch 母版完成住户/商户八张一级页面视觉收口与最小验收修复；保持首页冻结、Mock 与端级路由边界，完成 390×844 H5 运行回归，未进入阶段 02。 |
| 01.5 架构调整 | 2026-09-14 | 从单构建双 Profile 拆为住户/商户独立 App，保留共享 UI、Theme、Project 基础与 Mock 边界。 |
